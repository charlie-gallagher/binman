#include <stdio.h>
#include "header\\bin_error.h"


void print_errors(int error) {
    switch (error) {
        case : {
            fprintf(stderr, "\n");
            break;
        }
        default: {
            fprintf(stderr, "Unknown error code\n");
            break;
        }
    }
}
